% Configuration File
% This file is \input by Thesis.tex, so the values below drive the document.
%
% LIVE   - the value is used directly; change it here and recompile.
% MANUAL - LaTeX cannot read the value from here (it is a document class
%          option or an \include switch); the comment says where to change it.

% ========================================
% THESIS INFORMATION
% ========================================
% LIVE - title page and both abstracts
\newcommand{\thesisTitle}{Your Thesis Title Here}
\newcommand{\thesisTitleIT}{Il Titolo della Tua Tesi} % Italian abstract
\newcommand{\thesisAuthor}{Your Name}
\newcommand{\thesisDegree}{Master of Science}
\newcommand{\thesisDepartment}{Department Name}
\newcommand{\thesisUniversity}{University Name}
\newcommand{\thesisDate}{Month Year}
\newcommand{\thesisLocation}{City, Country} % MANUAL - not shown on the default title page

% ========================================
% SUPERVISOR INFORMATION
% ========================================
% MANUAL - the default title page does not print supervisors. To show them,
% uncomment the two marked lines in the titlepage block of Thesis.tex.
\newcommand{\supervisorOne}{Prof. First Supervisor}
\newcommand{\supervisorTwo}{Prof. Second Supervisor} % Optional

% ========================================
% FORMATTING OPTIONS
% ========================================

% LIVE - line spacing (1.25 for 1.5 spacing, 1.667 for 2.0 spacing)
\newcommand{\thesisLineSpacing}{1.667}

% LIVE - font size for chapters and sections (in pt)
\newcommand{\chapterFontSize}{14}
\newcommand{\sectionFontSize}{14}

% LIVE - page margins
\newcommand{\leftMargin}{25mm}
\newcommand{\rightMargin}{25mm}
\newcommand{\topMargin}{25mm}
\newcommand{\bottomMargin}{25mm}

% ========================================
% DOCUMENT OPTIONS
% ========================================

% MANUAL - a document class option, so it must be edited in the
% \documentclass line at the top of Thesis.tex.
\newcommand{\printingSide}{twoside}

% MANUAL - \include cannot be placed inside a conditional, so comment or
% uncomment the matching \include line in Thesis.tex instead.
\newcommand{\includeItalianAbstract}{true}
\newcommand{\includeAcknowledgments}{false}

% ========================================
% BIBLIOGRAPHY OPTIONS
% ========================================

% LIVE - true  : entries come from References.bib, formatted by \bibliographyStyle
%        false : entries are written by hand in Bibliography.tex (default)
% Nothing else needs editing; the whole document follows this switch.
\newcommand{\useBibTeX}{false}

% LIVE when \useBibTeX is true - e.g. plain, abbrv, alpha, unsrt, apalike, IEEEtran
\newcommand{\bibliographyStyle}{plain}

% ========================================
% CUSTOM COMMANDS AND ABBREVIATIONS
% ========================================

% Add your frequently used commands here
% Example: \newcommand{\mycommand}{definition}

% Degree symbols and superscripts
\def\dg{$^\circ$}
\def\2{$^2$}
\def\3{$^3$}

% Add more custom commands as needed
